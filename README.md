# HANN Model Simulation & Analysis

This repository contains code related to the paper:

A Thalamus–Brainstem Attractor Network Drives History-Biased Decisions

The code provided here focuses on the model simulation part of the study. The scripts for model simulation of the hierarchical attractor network are implemented in Python (using Jupyter notebooks) and are organized to reproduce specific analyses and visualizations of neural activity trajectories. 

---

## Quick start (recommended for users)

The code runs with Python 3.11.

You may either use an existing environment or create a clean conda environment.
---

### Option A — Use an existing Python 3.11 environment
#### 1. Install the package

From the repository root:

```bash
pip install -r requirements.txt
pip install -e .
```

#### 2. Launch Jupyter

jupyter notebook

Open the `notebooks/` folder and run the notebooks in order.

---
### Option B — Create a clean conda environment (recommended)
#### 1. Create environment

```bash
conda env create -f environment.yml
conda activate motor_network
```
#### 2. Install the package

From the repository root:

```bash
pip install -e .
```

#### 3. Launch Jupyter

```bash
jupyter notebook
```

Open the `notebooks/` folder and run the notebooks in order.

---

## Running the analysis

There are **two notebooks**:

### Notebook 1 — run_example_trials_with_motor.ipynb

This notebook:

* runs example simulations
* generates a continuous trial sequence
* produces motor outputs
* saves simulation results to `figures/`

This notebook demonstrates the model behavior.

---

### Notebook 2 — plot_traces_and_PCA.ipynb

This notebook loads previously generated results and computes:

* trial-averaged neural activity traces
* avoiding distance statistics
* PCA of neural population activity

This notebook reproduces the analysis figures.

---

## Repository structure

motor-network/

notebooks/
run_example_trials_with_motor.ipynb
plot_traces_and_PCA.ipynb

results/       previously generated data
figures/

src/
network.py     neural network model
motor.py       motor readout and analysis
tools.py       utilities
**init**.py

requirements.txt
environment.yml
pyproject.toml
README.md

---

## Notes

* Tested with Python 3.11 and 3.10 
* CPU execution is sufficient
* GPU acceleration is optional
* Simulation is deterministic with fixed seeds

---

##  Environment note (Taichi / BrainPy compatibility)

This project depends on BrainPy, which requires:

```bash
taichi==1.7.0
```

If you already have Taichi installed, a newer version may cause compatibility issues.

To avoid unexpected errors, we strongly recommend one of the following:

**creating a new clean Python environment for this project**, or

downgrading your existing Taichi installation to taichi==1.7.0

For example:

```bash
pip install taichi==1.7.0
```

Running the code inside an isolated environment helps prevent conflicts with existing Taichi installations and ensures reproducibility of the simulations.

---

## Reproducibility

All results can be regenerated by:

1. running notebook 1 (simulation)
2. running notebook 2 (analysis)

The repository includes example results for quick inspection.

---

## Contact

If anything fails to run, please report:

* Python version
* OS
* error message

We will respond quickly.
